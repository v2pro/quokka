package docstore

import (
	"net/http"
)

func queryEntity(respWriter http.ResponseWriter, req *http.Request) {
	//path := strings.TrimRight(req.URL.Path, "/")
	//pos := strings.LastIndexByte(path, '/')
	//entityId :=req.URL.Path[pos:]
	//newEntityLookup()
}

